-- PS99 ADVANCED STATS HUD (FINAL BUILD)

repeat task.wait() until game:IsLoaded()

local Players = game:GetService("Players")
local CoreGui = game:GetService("CoreGui")
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local player = Players.LocalPlayer

-- PS99 modules
local Library = ReplicatedStorage:WaitForChild("Library")
local Client = require(Library.Client)
local MapCmds = require(Library.Client.MapCmds)
local EggCmds = require(Library.Client.EggCmds)
local PlayerPet = Client.PlayerPet
local Save = require(Library.Client.Save)

-- remove old
pcall(function()
    CoreGui:FindFirstChild("StatsOverlay"):Destroy()
end)

-- ================= GUI =================
local gui = Instance.new("ScreenGui")
gui.Name = "StatsOverlay"
gui.IgnoreGuiInset = true
gui.ResetOnSpawn = false
gui.Parent = CoreGui

-- BIG ASF BACKGROUND
local bg = Instance.new("Frame")
bg.AnchorPoint = Vector2.new(0.5,0.5)
bg.Position = UDim2.fromScale(0.5,0.5)
bg.Size = UDim2.fromScale(2,2)
bg.BackgroundColor3 = Color3.new(0,0,0)
bg.BorderSizePixel = 0
bg.Parent = gui

-- container
local container = Instance.new("Frame")
container.AnchorPoint = Vector2.new(0.5,0.5)
container.Position = UDim2.fromScale(0.5,0.5)
container.Size = UDim2.fromOffset(520,420)
container.BackgroundTransparency = 1
container.Parent = bg

-- helper label
local function label(y, size)
    local t = Instance.new("TextLabel")
    t.Size = UDim2.new(1,0,0,size)
    t.Position = UDim2.fromOffset(0,y)
    t.BackgroundTransparency = 1
    t.Font = Enum.Font.GothamBold
    t.TextSize = size - 4
    t.TextColor3 = Color3.new(1,1,1)
    t.TextXAlignment = Enum.TextXAlignment.Left
    t.Parent = container
    return t
end

-- UI labels
local title = label(0,40)
title.Text = "STATS OVERLAY"
title.TextSize = 32

local playerLbl = label(60,26)
local zoneLbl = label(90,26)

local diamondsLbl = label(140,26)
local rateLbl = label(170,22)

local coinsLbl = label(210,26)
local petsLbl = label(240,26)

local eggsLbl = label(280,26)
local timeLbl = label(320,28)

-- close button
local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.fromOffset(150,40)
closeBtn.Position = UDim2.fromScale(0.5,1)
closeBtn.AnchorPoint = Vector2.new(0.5,1)
closeBtn.Text = "CLOSE"
closeBtn.Font = Enum.Font.GothamBold
closeBtn.TextSize = 18
closeBtn.BackgroundColor3 = Color3.fromRGB(220,20,60)
closeBtn.TextColor3 = Color3.new(1,1,1)
closeBtn.Parent = container
Instance.new("UICorner", closeBtn).CornerRadius = UDim.new(0,8)

-- open button
local openBtn = Instance.new("TextButton")
openBtn.Size = UDim2.fromOffset(150,40)
openBtn.Position = UDim2.fromScale(0.5,0.9)
openBtn.AnchorPoint = Vector2.new(0.5,0.5)
openBtn.Text = "OPEN STATS"
openBtn.Font = Enum.Font.GothamBold
openBtn.TextSize = 18
openBtn.BackgroundColor3 = Color3.fromRGB(40,40,40)
openBtn.TextColor3 = Color3.new(1,1,1)
openBtn.Parent = gui
Instance.new("UICorner", openBtn).CornerRadius = UDim.new(0,8)

-- state
local visible = true
local startTime = os.clock()

local function setVisible(v)
    visible = v
    bg.Visible = v
    openBtn.Visible = not v
end

closeBtn.MouseButton1Click:Connect(function()
    setVisible(false)
end)

openBtn.MouseButton1Click:Connect(function()
    setVisible(true)
end)

-- format helpers
local function fmt(n)
    if n >= 1e9 then return string.format("%.2fB", n/1e9)
    elseif n >= 1e6 then return string.format("%.2fM", n/1e6)
    elseif n >= 1e3 then return string.format("%.2fK", n/1e3)
    end
    return tostring(math.floor(n))
end

local function fmtTime(t)
    return string.format(
        "%02d:%02d:%02d",
        math.floor(t/3600),
        math.floor(t%3600/60),
        math.floor(t%60)
    )
end

-- diamonds stat
local diamondsStat
task.spawn(function()
    local ls = player:WaitForChild("leaderstats")
    diamondsStat =
        ls:FindFirstChild("💎 Diamonds")
        or ls:FindFirstChild("\240\159\146\142 Diamonds")
end)

-- diamonds / min
local lastDiamonds = 0
local rate = 0
local rateClock = os.clock()

-- eggs hatched (session)
local eggsHatched = 0
local oldRequest = EggCmds.RequestPurchase
EggCmds.RequestPurchase = function(...)
    eggsHatched += EggCmds.GetMaxHatch()
    return oldRequest(...)
end

-- update loop
RunService.RenderStepped:Connect(function()
    if not visible then return end

    -- player
    playerLbl.Text = "PLAYER: "..player.Name

    -- zone
    local zone = MapCmds.GetCurrentZone()
    zoneLbl.Text = "ZONE: "..tostring(zone or "Unknown")

    -- diamonds
    if diamondsStat then
        local current = diamondsStat.Value
        diamondsLbl.Text = "💎 Diamonds: "..fmt(current)

        if os.clock() - rateClock >= 5 then
            rate = (current - lastDiamonds) / ((os.clock() - rateClock) / 60)
            lastDiamonds = current
            rateClock = os.clock()
        end

        rateLbl.Text = "💎 Rate: +"..fmt(math.max(rate,0)).." / min"
    end

    -- coins
    local save = Save.Get()
    local coins = save and save.Currency and save.Currency.Coins or 0
    coinsLbl.Text = "🪙 Coins: "..fmt(coins)

    -- pets
    local equipped = #PlayerPet:GetAll()
    local maxPets = save and save.MaxPetsEquipped or 0
    petsLbl.Text = "🐾 Pets: "..equipped.." / "..maxPets

    -- eggs
    eggsLbl.Text = "🥚 Eggs Hatched: "..eggsHatched

    -- time
    timeLbl.Text = "⏱ Time: "..fmtTime(os.clock() - startTime)
end)

print("✅ PS99 ADVANCED STATS HUD LOADED")
